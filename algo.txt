генерим конус (NxN)
генерим b: b[0:n-1] = 0, 0<=b(n)<=p-1
генерим крышку с, проверяя все определители на 0; каждый элемент крышки должен быть не меньше левого соседа; значения - от -p до +p включительно
sig = cx*, где x* = [0,0,0,..,b(n)/p]
f = [0,..,0, (b(n)+1)/p]
2 случая: -cx >= [-][sig] (сверх) и cx >= [sig] (снизу)
